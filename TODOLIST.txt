etcd可视化操作需求
一期：
1、接受etcd地址、端口信息创建etcd连接
 1.1 连接etcd
 1.2 断开连接
 1.3 查询连接状态
2、list all keys
3、根据key获取内容
4、支持前端对内容进行json/yaml格式化和反格式化

接口设计：
    POST api/v1/etcds
	req body:
	    name:
	    host:
	    port:
	resp body:
	    code:
	    message:
    POST api/v1/etcds/{etcdname}/connect
    POST api/v1/etcds/{etcdname}/disconnect
    GET  api/v1/etcds/{etcdname}/status
    List api/v1/etcds/{etcdname}/keys
	resp body:
	    code:
	    message:
	    data:
    GET api/v1/etcds/{etcdname}/values?key={key}
	resp body:
	    code:
	    message:
	    data:
二期：
